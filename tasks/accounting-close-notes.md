# Next accounting build checkpoint

Current branch: codex/accounting-books. Completed checkpoint 6d9cae6 contains reconciliation, clearing timelines, month close, filing/restatement, account lifecycle and export v3. Its migration 20260905225711_accounting_reconciliation_close.sql is committed and immutable, following 20260905203346 and 20260905215106. The user has only confirmed applying the first migration. No live DB writes were made. Draft SQL module files from this checkpoint were removed after packaging.

Current uncommitted work: schema parity tooling. accountingTestDb accepts migrations/canonical; accountingCanonicalSql extracts marked accounting blocks; verify-accounting-schema compares actual PostgreSQL catalogs for tables, columns/defaults, constraints, indexes, functions/grants, triggers and policies. Seven comparisons pass. This replaces naive whole-block text matching, allowing a later migration to replace a function while the canonical schema edits its sole definition in place. Add this test to validation for future migrations.

Next packet: historical report verification, import continuation, transfers and remaining daily work.

Historical verification design:
- Source evidence must remain independent of the displayed book values. Do not prefill expected controls from the books.
- Verify one calendar-year scope at a time, explicitly allowing partial-year coverage. Require exact monthly P&L controls within that scope and mapped year-end/full trial-balance account controls, including zero-balance active bank/card accounts. Compute total balance-sheet equity as direct equity + retained + current-year profit. Source basis must be cash and documented.
- Save immutable acct_history_checks with source document, exact controls, account controls, current financial revision and explanation. The existing posted-entry callback already invalidates checks ending on/after a changed financial date and marks later import coverage unverified.
- Coverage becomes verified only when checks cover every year portion of a completed batch and every economic group is posted or proven duplicate. A cancelled partial batch cannot remain a dead end: add import.resume to restore staging/review at its saved checkpoint. Block cancelling completed/verified batches through the command boundary.
- Add typed immutable dispositions for excluded groups. Unsupported/economic exclusions block coverage. An annual closing normalization needs source support, only nominal/opening-retained accounts and amounts that exactly reverse the relevant year's canonical nominal activity. Never accept an arbitrary note as proof that an economic group can disappear.
- Historical baseline locking is distinct from locally completed statement reconciliation. Create historical_baseline report snapshots and explicit close records referencing parity proof, then allow the period guard to recognize that evidence. No invented statement matches. Reopen/invalidation retain the original baseline proof.
- Ordinary close checks now inspect all prior drafts/imports through the cutoff, so unresolved history cannot silently feed later opening balances.

Technical next changes:
- Create a new draft schema module outside migrations; test it with PGlite. Only package the next migration once ready. No uncommitted migration exists now.
- Modify acct_operate/close guards in the new migration when needed. Edit their canonical definitions in place when packaging, and verify catalogs with test:accounting:schema. Keep committed migrations immutable.
- Transfer groups: same-date uses one journal, different dates use two via configured transit, preserving actual bank/card dates and sign; amounts exact. Link their transit lines through dated bounded clearing allocations. Posting/reversal is atomic and all command receipts replay safely.
- Remaining daily work includes partial bank allocations, redundant-draft resolution, rules/aliases, statement CSV item entry, searchable choices, entry-ID copy, amount filters and saved-view range labels.

Current test state:
- money/contracts 591; foundation DB 73; workflows 36; CSV parser 21; import persistence 15; document permissions 11; reconciliation/lifecycle 20; clearing 13; period/filing/backup 18; actual PostgreSQL concurrency 9; HTTP/private evidence 18; existing tax 88; schema parity 7. TypeScript, accounting ESLint and production accounting build passed before the schema-tooling change.
- The loopback PostgreSQL fixture (127.0.0.1:5447/accounting_test) has all three migrations' functions/tables, plus synthetic data only. It was initially installed from draft modules; a few later lifecycle CHECK constraints have only been tested in fresh PGlite, so use a fresh actual-PG rebuild during final restore/integration acceptance. sync-accounting-postgres refreshes functions/grants, not all table/trigger DDL.
- prepare-accounting-postgres currently needs improvement before fresh real-PG use: roles already exist at cluster scope, and storage stubs/policies plus profile purposes must be initialized correctly. Keep marker/loopback guards.
- The old concurrency test was updated to test proper close commands: a close waiting behind a posting rejects a stale revision, then a retried close succeeds; a writer waiting behind a completed close rechecks the lock. Cleanup is a documented reopen and reversals.

Browser/runtime state:
- User localhost app remains on 3000. Do not kill or modify its .env. User requested no Playwright; CUA only.
- Synthetic editable app 127.0.0.1:3108 uses APP_ENV=test, NEXT_PUBLIC_DEMO_MODE=true, NEXT_PUBLIC_ACCOUNTING_ENABLED=true, ACCOUNTING_TEST_DATABASE_URL to marked fixture; exec session 58794. Separate .next-accounting-test output.
- Read-only older demo on 3107 is ours and can be stopped if needed.
- CUA variable testTab is tab2/browser1, provider browser-use:98f1659e-fd70-4ea0-95a4-25facbb35e95. User tab1/browser1 is localhost3000; preserve it.
- Most recent UI acceptance: Savings February 2026 reconciliation created, original synthetic CSV attached, one $500 item matched to posted Transfer reaches savings, and completion confirmed. All three differences zero; completed proof visible. Latest AX indices are stale after HMR, get fresh state.
- Synthetic statement document ID 6b679a47-567d-4802-9f94-fa3fc23fc507, named synthetic-savings-statement.csv. Other synthetic HTTP CSV docs exist. Original private files under LOCALAPPDATA/CodexAccountingTest/evidence.

Known follow-through risks:
- A document uploaded within a form increments the general financial revision, so revision-based forms may need refreshed review after upload; do not silently overwrite a stale financial review.
- Full books document bundle/restore not implemented yet. Export v3 now includes all closed-book tables and exact string money fields, but no file bytes.
- Statement create/edit is currently create + manual item entry, remove unmatched item, cancel and recreate for header corrections. Add CSV and reusable replacement/copy ergonomics.
- Account lifecycle reason is in the command receipt/audit; no meaningless annotation is created. Zero final statement plus zero book balance and no later posted/draft activity are required.
- AI/MCP/public REST remain deferred. Invoice lifecycle, payroll registers, SimpleFIN, full reports/manual assets/loans/cashflow/contractors, tax links, portable restore and operational gates remain open. Actual Wave/Patriot exports and three real parallel closes are external acceptance dependencies, never fabricated.

Work started after the checkpoint notes above:
- New uncommitted draft `supabase/schema/accounting-history.sql`, not installed in the browser fixture or canonical schema/migrations. It adds immutable excluded-group dispositions, annual-closing validation, one-year monthly/account/balance-sheet control preview, history verification, import.resume, and a copied replacement acct_operate dispatching the new command family. `scripts/verify-accounting-history.ts` loads it after committed migrations; 11 assertions pass.
- Source-control nuance: canonical books never close nominal accounts annually. Compare income/expense account amounts over the selected year scope, balance-sheet asset/liability/capital amounts as of its end, and total equity including prior retained + current-year profit. Exclude the opening-retained system account from direct source account controls; aggregate equity checks its accumulated effect. Monthly source totals catch inter-month offsets. Partial-year scopes are explicitly labeled.
- `acct_history_preview(from,to,monthly,accounts,totals)` defaults empty controls and is owner readable; monthly controls require exact subranges and income/expense/net cents; accounts use signed debit-positive amounts; totals require assets, liabilities and total equity. All source-control values must be supplied independently, never prefilled from actual book values.
- New acct_operate replacement is currently only in the draft module. When packaging, edit its existing canonical definition in place and append only new definitions from the history module. Schema-catalog tests now permit this safely. Do not append a second canonical acct_operate definition.
- Historical baseline lock commands, read/UI/API contracts, typed disposal UI, import cancel-completed restriction, exhaustive source-group/normalization tests and deeper monthly per-account controls still need implementation.
- The draft source error gate treats unsupported/economic exclusions as incomplete. Only documented, excluded annual closing groups can pass: nominal/opening-retained accounts only, balanced, year-end or following Jan1, each nominal amount exactly offsetting canonical activity in that year. A SQL NULL-handling bug in purpose checks was corrected with coalesce.
- Tooling caution: JavaScript String.replace replacement strings interpret dollar sequences and can corrupt PostgreSQL $$ bodies. Use a replacement callback or direct concatenation for SQL function text. A copied draft function initially had single-dollar delimiters; corrected and tests passed.
- Last progress update said the first historical control tests pass and the import-review UI is next.

Latest draft history additions:
- Historical baseline lock now creates explicit historical_baseline snapshots and close records, with source parity proof. It locks only full calendar months inside the accepted scope, rejects future months or prior unverified active imports, and never fabricates statement matches. The draft replaces acct_close_period_guard to recognize these validated baseline records. Ordinary reopen already preserves them.
- acct_history_view returns checks, invalidation state, excluded groups/dispositions and year classifications. A document guard retains disposition evidence. The draft acct_operate also refuses cancelling completed/verified imports.
- verify-accounting-history.ts now passes 12 assertions, including baseline lock/reopen followed by financial invalidation. This code remains in a draft schema module and is NOT applied to the persistent browser database or canonical schema/migrations.
- Initial TS contracts are in src/lib/accounting/history.ts, not yet wired to workflows.ts or API/UI. historyPreviewSchema provides exact monthly P&L, signed account controls and asset/liability/total-equity controls. Source amounts should never be prefilled from actual books.
- The inactive-input convention is consistent: cancelled batches with no applied groups are ignored for parity/close, allowing a wrongly mapped unused batch to be abandoned and reimported correctly. Any partially applied cancelled batch remains incomplete until resumed.
- Next build the API preview/read routes and historical control UI in Manage > Imports, then deeper source coverage/disposition/replay tests. Review whether monthly per-account comparisons should supplement existing monthly totals + annual account comparisons for category timing differences.

Latest UI/integration progress:
- Historical contracts are wired into workflows, API GET view=history, and read-only POST /api/accounting/history. Owner origin/body limits apply. local-test-client whitelists both RPCs.
- New AccountingHistory UI is in Manage > Verify history. It has independent source-document selection, cash-basis confirmation, year classification, blank monthly/source-account/BS control inputs, exact comparison, explicit partial-year labels, saved parity records, historical-baseline lock review and excluded-group disposition review. Range changes clear source inputs; refresh preserves entered controls but invalidates the prior comparison.
- Import UI now exposes cancellation with a reason and resume for cancelled/failed batches. Error handling around reload callbacks could still be improved to avoid an unhandled fetch failure.
- Source dispositions are now append-only versioned records per group, so an initially unsupported record is not trapped forever. acct_history_review_invalidations records when a new disposition invalidates earlier checks. acct_history_check_current combines financial and review invalidations. Re-review requires affected periods reopened first. The affected-date expression is still overly conservative (previous year even for non-Jan1 groups); narrow it to the relevant year/previous-year closing when polishing.
- The draft history module is NOW installed in the marked actual-PG browser fixture (tables plus replacement functions). Later function edits were refreshed directly from draft function blocks, not through migrations. No canonical/migration packaging yet. No new migration exists in the working tree.
- Historical preview rejects noninteger/exponent/fractional-cent controls, exposes scope_ended/entity_verified, and requires the scope through date <= today for readiness. history_view now returns eligible_months and locked_months for visible completion feedback.
- Current historical checks: 12 assertions pass. TypeScript and accounting ESLint passed before the latest small display-field changes. No broader suite/build rerun is justified until this packet finishes.
- Browser acceptance completed: synthetic January source report uploaded (document 9e968ed3-97a7-427e-a2e6-eb57fb47f161, synthetic-january-control-reports.csv), exact independently specified controls entered, 2026 S-corp classification confirmed, saved verified partial-year record, and January historical baseline locked. Snapshot f8bed0ed-4c9d-4671-b71a-00f32eb5d35b. Need REOPEN January in the UI before later synthetic financial fixture changes.
- Current CUA testTab is in Close books, month 2026-01. Latest AX has button88 Review reopen impact. Get fresh AX after HMR.
- Found a UI labeling issue: Close books labels historical baseline snapshots as ordinary Completed close and shows closed alongside outstanding local-statement checks. Next add a clear historical-baseline banner/status and label those saved records correctly. close-history SQL already returns proof.kind/history_check_id; extend TS CloseHistory close-record type and render these fields. Then exercise reasoned reopen and retain the snapshot.
- Useful UX follow-through: save/resume unfinished source-control worksheets or import their control CSV, so lengthy multi-year manual reviews do not lose input on navigation. Not implemented yet. Statement CSV item entry also remains open.


Checkpoint 4: packaged history into new `20260905233800_accounting_historical_verification.sql`, canonical functions replaced in place, draft removed. Schema catalog parity 7, history 12+13, close20/clearing13/period18, TypeScript/lint passed. Production build running session9928. Added backup v4 histories. No live migrations applied. Next: transfers/rules/partial bank allocations, then source subledgers, reports/tax/restore.

Checkpoint5 transfers packaged in 20260905234949. Browser synthetic $125.50 March31/April2 posted then reversed. Localfixture account11 Payment transit purpose transfers_in_transit. New partial bank matching is next. No live writes.

Checkpoint6 partial matching packaged in new 20260906000213, canonical bank_matches unique source constraint removed in place. Backup v5. Fresh banktest23. Browser full split-match passed; May batch95f3d798-392e-4f89-84df-010cfe00237b group e927a96a-6e67-482c-95c7-d4ac20c085c7, complete allocation to 200+300 receipts, redundantdraft discarded. Localfixture extraaccount12 Uncategorized receipts purposeuncategorized_income. Currentbrowser bankmatchmodal fullyallocated. Actualfixture moduleinstalled. Remaining fullbuild work still rules, statementCSV, openingretained guard, SimpleFIN, invoices, Patriot, reports/registers, taxadapter, portablebackup/restore/acceptance.

Checkpoint6 regression all passed incl productionbuild, concurrentPG9, HTTP18, fullfocusedcore suites. Ready to commit. User noPlaywright remains binding; all visualchecks CUA. No independent tasks spawned. Continue with rules/aliases and safe opening workflow; large laterpackets still open per buildplan.

Checkpoint7 retainedreview migration20260906001354 packaged, backupv6. Manualretained.post exactsourcecontrol/doc required, existingearlierhistory forbidsnewmanualopening. Historicalcashimportopening auto retains sourcefile+exactsourcecontrols butannualnominalclosingblocked; fullhistorycoverage stillseparate. Entry.correct requires retained_review controls/doc ifreplacementusesretained. Basic14tests passed, addedduplicateopeningtest pendingrerun. Actualfixturemodule installed; account13 Opening retained earningspurpose. Browser syntheticentry bd935143-d615-4274-b654-8fb286d48b8b, doc6494f6a9-8ef5-4a69-9ba0-c62c3818079e synthetic-opening-control.csv. Posted2024Dec31 $500 viaUI thenreversedto0same day. CurrenttestTab thisentryURL2024range. UIlastinputdifferenceimprovement afterscreenshot notyetre-viewed. NeedfinalTS/lint/testretained/build check thencommit. Nextpacket rules/aliases andstatementCSV, thenlargeinvoice/payroll/SimpleFIN/reports/tax/restore. User requestedallbuild no stopping.

Checkpoint7 finalchecks passed: retained15, history12+13, import21+15, bank23, close20/13/18, schema7, TS/lint/build. Ready to commit. Currentworkingmigration newlycreatedneveredited.


Next statement CSV design (not implemented yet): existing POST /api/accounting/imports phase=inspect/preview already returns all parsed bankGroups and exact fileHash/mappingHash, raw rows, stable external_id/fingerprint. Reuse this pure parser for statement files; it does not stage ledger imports. Existing reconciliation.items command supports atomic chunks of100 but no persisted source identity. Add immutable statement-source rows keyed by reconciliation+external_id, original document/file hash/mapping/raw/financial fields and retained item UUID (intentionally no FK if unmatched item deletion is allowed, or use explicit restore state). Duplicate same identity/financial fields skips; changed financial fields reject; a deleted prior imported item requires explicit restore, never silently re-add. Fill free ordinal slots below declared_count to permit removed-item repair. Source doc must be available and hash agree. Statement import persists chunks with version checks, maintains source proof and never creates journals. Extend owner dispatcher and backup. Add source/header evidence archive guard. Header edit in_progress only, preserve old header/document in amendment history, validate all current items fit range/count, predecessor continuity and no overlaps; clear reviewed opening allocations when the opening/from changes so opening must be reviewed again. UI should reuse CSV mapping controls, show independent declared controls vs parsed count/debit/credit, preview rows/errors, upload/retain original CSV and support retry/resume. Must not overwrite independent statement totals from CSV automatically. Existing StatementCreate can be generalized for amend/replacement prefilled dates/controls. Read schema guards before changing.

Statement packet started: draft schema/accounting-statement-files.sql added immutable files/itemsource/headeramendment records, statement.import stableidentity/partialretry/explicit restore, statement.amend controls validation and opening-reset, evidencearchive guard, sources read, acct_operate dispatch + backupv7. NOT TESTED, no migration created, not installed actualfixture. Nextwrite verify-accounting-statement-files.ts and UI/API/contracts. ExistingCSVparser API reused; fixed UTF8BOM preservation in api/imports/route.ts, HTTPtestnow21passed. NewstatementSQL has noitemFK intentionally retainsdeletedunmatcheditemidentity andrequiresrestore_removed torestore. source financialchangesreject. Headeramend validatesitemsdates/count/predecessor/overlap, keepsprior/newdoc. CheckSQLguards/retries withtests beforeinstalling.


Checkpoint8 statement files packaged in migration20260906003706, backupv7. DB24, HTTP8, generalHTTP21, schema7, close20/13/18, retained15, history12+13, TS/lint/build passed. Browser imported same attachedCSV through mapping/preview; counts1 and +500 matched, retry keptoneitem. Statement3a84672b-a150-4ea5-b002-4d034ef60488 May2026 Checking, docbfddb283-e11a-401e-a44c-46b65925a4b0 synthetic-may-statement.csv. Headernotes amended inUI withreviewreason andbefore/after visible. No journalscreated. CurrenttestTab successfulCSVimportmodal. Actualfixturemodule installed. Newmigration createdonce, neveredited. Next rules/aliases thenSimpleFIN/invoices/payroll/reports/tax/restore fullscope remains.


Checkpoint9 rules migration20260906005815 packaged once (not edited) and canonical4functions replaced in place; backupv8. DraftSQLremoved. DB28, corecontracts612, HTTP22, schema7, bank23, imports21+15, history12+13, retained15, statement24, workflow36, close20/13/18, TS/lint/build passed; latestfinalformat/lint/build pending. UIacceptance complete creatinghostingrule bounds$10-$25 acct1 category6 prefixacmehosting, alias Synthetic softwarevendor, enableversion2, select2drafts$39.99, applyandreadhistory. Syntheticdraft IDs952657ed-583d-493a-ba1b-4739733f887b ($19.99 June10),3cd79f6c-0c6a-4673-8471-5289ccab9d7c ($20 June15), both remain draft. Newacct14 Uncategorized costs purposeuncategorized_expense. Actualfixtureallmoduleinstalled, lastfunctionrefreshincludespausedarchivedrulefix. CurrenttestTabentry952657 June range shows readableappliedruleevidence. No realDBchanges. Commitwhenfinalchecksfinish. NextSimpleFIN withofficialprotocolverification andencryptedNodeworker; priorpostedsuggestions/contextualrulediscovery stillneedrefinement; largelaterinvoice/payroll/report/tax/restorepacketsremain. Newcentsschemapipes preventchainedBigIntrefinementthrow oninvalidinput (rules/statement/close), APIreturns400. Evidence rawsourceandstoredruleJSONmasked.

Checkpoint9 final lint/build passed after input-validation regression fixes and readable rule evidence. Ready to commit.


Checkpoint10 SimpleFIN packaged once in new migration20260906015114, canonical3existingfunctions replaced in place; newfeedmodule appended. No working draft SQL remains and the new migration has never been edited. Backupv9 includes allfeedtables exceptsecrets. Protocol86, DB69, engine/crypto35, schema7, close20/13/18, history12+13, bank23, imports21+15, retained15, statement24, rules28, actualPGconcurrency9, HTTP30, TS/lint/build passed. Finalcore/workflow/HTTPcheck pending if not yet captured. Need commit this checkpoint after final housekeeping.

No live banking requests or tokens used. NodeAPI feedConfiguration forbidsnetwork for demo/test DB, worker also needs globalenabled+secret and owner perconnection scheduling. Restrictedservice-roleacct_feed_server cannotcreate/postjournalorchangeownermapping. One-timeclaims have started->transmitting beforeexternalclaim; uncertainreplaysblocked. Exacthostsbridge.simplefin.org/beta-bridge.simplefin.org, DNSpublic+pinning, HTTPSno redirects, version2, posted epochendexclusive, 5dayoverlap90daymax, 24requestglobalconnquota, max4requests/run, accountpartialprogress, sourcegaps, properpending/zerohandling, wrongcurrencyneverstages, sourcechangesexceptions. Keep encryptionkeysseparateSIMPLEFIN_ENCRYPTION_KEY[_Vn]. Docs admin/tasks/accounting-simplefin.md.

Actualfixture now all10modulesfunctions plusnewwindowimmutabletrigger installed; laterDBconstraints fromearliermodules still needfreshrebuild/restoreacceptance. Syntheticconnection60000000-0000-4000-8000-000000000001 nameSyntheticcompanybanking hasfakecipher andnetworkdisabled. Checkingcanonicalfeed4f2b76d2-b651-4488-843d-a92f30324a9c mappedGL1, history2026Jun1UTC, checkpointJul1exclusive, signs1/1. CardmappedinUI GL3 signs movement1,balance-1, sameJune1UTC, no transactionwindow. Personal savings excludedinUI. Sourcebalance1273725checking, carddebt3000, availableNULL. 1pendingobservation$5 retainednevercreatesdraft. Syntheticbankdraft6091579b-4dba-4bac-ae9b-29852eaeb092 June18-$42.75check/uncatcost14, source simplefin/synthetic-feed-movement-1; addedviaUIprepare+approvedraft. Now3June drafts includingprior2rule drafts. All remainunposted. Newlycreatedfeedbatch stillapplying; actualid canquery ifneeded. NofinancialJuneprofitimpact.

CUA testTab(tab2/browser1) currentJune1-30 URL withentrymodal609157 open (URLstillnoentryparam; linkexists). Sourceevidenceexpanded, close38 fromlastAX. Userlocalhost3000 untouched. CurrentgetAXStateAndScreenshot inspectednormaldraft/sourceproof. Editabletestapp3108 execsession58794 continues; earlier3107oursunused. Productionbuild90249donepassed; regsessions22503/1571donepassed. No runningtestprocessotherthanservers. Actualmigrationrefresh script completedafterpack, allsamefunctiondefinitions.

Newclosecheckunreviewed_feed_movements, bankmodeimports checkedposted/matched notannualcoverage; mappedfeedaccounts requirestatementproof fromhistory_start. There is a separate readonly acct_feed_gap_covered using range_agg of completed statements/currenthistorycontrols. Fullhistorybaseline still independentcontrolgate. NewSQLusesno worker impersonation: serverrequested_by nullable+actor_kind perrun. Unreviewedrawfeedactivitydoesnotmutatefinancialrevision; close rechecks undercompanylock. Ownerfeed.prepare incrementsrevision throughacct_operate, otherfeedcommands keepfinancialrevision.

Next fullbuild: invoice lifecycle/sourceCRM durable revision outbox/inbox, snapshotbootstrap/tombstones/outoforder/retries/nightlyconvergence, settlements/refunds/aging/historicalreceiptmatching. Then Patriotpayrollcomponents/clearing, extendedreports/manualassetsloans/contractorworksheet, opt-intaxadapter, portableDB+documentsbackup/cleanrestore/operationalacceptance. AI/MCP/publicRESTdeferred. Existingpersonalpagesmanualdatapreserved. ActualWave/Patriotexports and3actualparallelcloses remainexternalacceptancegates, notimplementedpretenddata. Do notfinalorstopaftercheckpoint. No subagents allowed withoutuserrequest.

Checkpoint10 final core612/workflows36/HTTP30 passed. Production build and lint/TS were already green. New migration unchanged after initial creation. Ready to commit. Initial next-packet inspection found CRM paid-only emit_invoice_webhook and one-attempt dispatcher; admin webhook only handles paid/updated/deleted personal-income reconciliation. Must preserve that path while adding a separate full-lifecycle accounting event and durable inbox. No app AGENTS.md found by rg. app/supabase/schema.sql canonical; app/supabase/migrations contains committed webhooks migration20260725154128. Source app and admin migrations need separate commits. No new invoice work files yet.
